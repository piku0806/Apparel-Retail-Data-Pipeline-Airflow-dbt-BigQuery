# Extra Python packages needed on top of the base Airflow image.
# Wired into docker-compose via the AIRFLOW__ _PIP_ADDITIONAL_REQUIREMENTS env var below,
# and also usable directly (`pip install -r requirements.txt`) for local/non-Docker runs.

faker==26.0.0
pandas==2.2.2
google-cloud-bigquery==3.25.0
dbt-core==1.8.4
dbt-bigquery==1.8.2
